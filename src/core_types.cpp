#include "core_types.h"
#include <cmath>
#include <algorithm>

namespace aiaudio {

// This file is intentionally minimal since most functions are inline in the header
// Only add implementations here if they need to be non-inline for some reason

} // namespace aiaudio
